# set the default behavior, in case people don't have core.autocrlf set.
* text=auto

# .gitattributes in project root (you can use 'package*.json text eol=lf')
package.json text eol=lf
package-lock.json text eol=lf